#include<iostream>
#include<string>
#include<memory>

using namespace std;

namespace w_ptr //weak_ptr has no ownership of the pointed object, which is like a raw pointer. But it cant be used directly like a row pointer to point the member(weak_ptr->member)
{

}